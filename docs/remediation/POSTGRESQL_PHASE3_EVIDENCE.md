# PostgreSQL production readiness remediation — Phase 3 evidence

Generated: 2026-08-26

## Scope and custody

- Branch: `remediation/postgresql-production-readiness`
- Protected release commit: `30c9568012879319675a4c86eeb712519f61dfe3`
- Phase 3 scope: normalize the core schema definition and bootstrap boundary
- Repository SQL portability: not completed; deferred to Phase 4
- Legacy data conversion and migration runner: not completed; deferred to Phase 5
- Production deployment or external database access: none

## Change evidence

| Objective | Evidence |
| --- | --- |
| Single core schema source | `database/schema.py` owns the core table and index definitions. |
| Backend-neutral DDL execution | `initialize_schema()` executes individual statements through `DatabaseBackend.session()`, so it does not require SQLite `executescript()`. |
| Dialect boundary | SQLite uses `INTEGER PRIMARY KEY AUTOINCREMENT`; PostgreSQL uses `BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`. No other core DDL is backend-specific. |
| Version marker | `schema_migrations` records `CORE_SCHEMA_VERSION = 1` idempotently. |
| Compatibility bootstrap | `database.models.create_tables()` delegates to the normalized schema contract. |
| Legacy boundary preserved | Existing SQLite-only migrations and direct repository SQL remain unchanged for the later portability/data-conversion phases. |

## Validation

Focused Phase 3 validation: 13 passed. Coverage includes SQL-shape checks for
both backends, SQLite initialization of the complete core table inventory, and
the legacy `create_tables()` compatibility API. No PostgreSQL server,
credentials, or production database was contacted.

The full clean-clone regression remains the Phase 2 baseline until the Phase 3
working-tree changes are included in the next clean validation checkout.
