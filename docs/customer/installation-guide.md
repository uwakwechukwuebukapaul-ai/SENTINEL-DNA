# Sentinel DNA Installation Guide

Deploy the application with a managed PostgreSQL database, Redis, TLS termination, secret-manager-backed credentials, and separate API/worker processes. Run migrations before enabling tenant traffic. Verify `/api/monitoring/health` with an authenticated tenant administrator before onboarding customers.
