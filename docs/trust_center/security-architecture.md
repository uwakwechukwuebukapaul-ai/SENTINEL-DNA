# Security Architecture

Sentinel DNA isolates customer data by organization context, applies RBAC and CSRF protection, records audit events, and exposes observability metrics. Production deployments should use TLS, managed secrets, PostgreSQL, Redis, and encrypted object storage.
