const providers = [{ id: "entra", name: "Microsoft Entra ID", icon: "▦" }, { id: "okta", name: "Okta", icon: "O" }, { id: "google", name: "Google Workspace", icon: "G" }];
export function initIdentityProviderSelectors() { document.querySelectorAll("[data-idp-selector]").forEach((root) => { providers.forEach((provider) => { const button = document.createElement("button"); button.type = "button"; button.className = "sentinel-idp-button"; button.innerHTML = `<span class="idp-icon" aria-hidden="true">${provider.icon}</span><span>${provider.name}</span><span class="idp-status">Optional</span>`; button.addEventListener("click", () => { root.querySelectorAll(".sentinel-idp-button").forEach((item) => item.classList.remove("is-selected")); button.classList.add("is-selected"); root.dispatchEvent(new CustomEvent("identity-provider-selected", { detail: provider })); }); root.append(button); }); }); }
