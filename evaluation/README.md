# AI Investigator Evaluation

The evaluator uses only deterministic Customer Zero fixtures. Supply an existing investigation result to `InvestigationEvaluator.evaluate()`; the evaluator never runs investigations or production actions.
