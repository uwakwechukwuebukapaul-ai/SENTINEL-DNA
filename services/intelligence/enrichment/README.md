# Threat intelligence enrichment

This package provides a deterministic, offline enrichment boundary for investigation artifacts. It makes no external calls and does not replace the investigation risk engine; callers may use its structured results as evidence before reasoning.
