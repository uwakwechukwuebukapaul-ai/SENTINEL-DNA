# AI incident response orchestration layer

This layer turns investigation outcomes into tenant-scoped, auditable response
recommendations. It preserves SOAR ownership and only simulates execution;
every action requires an explicit approval request and approval decision.
