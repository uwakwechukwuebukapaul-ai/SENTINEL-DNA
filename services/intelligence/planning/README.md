# Investigation planning

The planner produces deterministic, explainable plans from synthetic security events and optional enrichment results. It is an advisory pre-execution layer and does not replace `InvestigationCoordinator` or `InvestigationOrchestrator`.
