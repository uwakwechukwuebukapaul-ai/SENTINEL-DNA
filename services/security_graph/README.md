# Security knowledge graph

This deterministic in-process graph connects tenant-scoped security entities and relationships for enrichment and investigation context. It does not replace source repositories or execute actions. Future adapters may support Neo4j, graph embeddings, attack paths, threat actor profiles, and graph-aware AI planning.
